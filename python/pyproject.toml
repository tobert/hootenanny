[project]
name = "hootenanny-python"
version = "0.1.0"
description = "Python workspace for hootenanny model services"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["hootpy", "services/*"]
